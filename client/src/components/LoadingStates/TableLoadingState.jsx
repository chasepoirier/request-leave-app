import React from 'react'
import { TableLoadingWrapper } from './Styled'

const TableLoadingState = ({ children }) => (
  <TableLoadingWrapper>{children}</TableLoadingWrapper>
)

export default TableLoadingState
