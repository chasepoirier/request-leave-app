import reducer from './reducers'

import * as supervisorOperations from './operations'
import * as supervisorSelectors from './selectors'

export { supervisorOperations, supervisorSelectors }

export default reducer
